Feature: 多栏工作台
  用户在同一项目内组织文稿、查阅资料和查看版本，同时保留当前写作内容。

  Scenario: WB-01 在概览与文稿之间往返保留写作状态
    Given 当前文稿包含未保存修改和一个选区
    When 用户查看项目概览后返回文稿
    Then 正文、文档身份、选区和撤销历史仍属于原文稿
    And 概览展示真实项目和文档信息

  Scenario: WB-02 项目轨的快捷切换遵循保存结果
    Given 用户有多个项目且当前文稿未保存
    When 用户从项目轨选择另一个项目
    Then 切换通过现有项目会话入口执行
    And 保存失败时保留原项目并显示错误

  Scenario: WB-03 文献选择与插入分离
    Given 当前文稿可编辑且文献库有可选条目
    When 用户选择一条文献
    Then 显示该条目的标题作者年份和可用详情
    And 正文不发生变化
    When 用户明确插入所选引用
    Then 引用写入原文稿的当前选区

  Scenario: WB-04 文献库变化拒绝过期选择
    Given 用户已选择旧库中的条目且一次查询尚未完成
    When 文献库身份发生变化
    Then 清除旧选择并显示当前库状态
    And 旧响应不能覆盖新库或触发正文插入

  Scenario: WB-05 工具与文档标签可用键盘切换
    Given 用户通过键盘聚焦当前标签
    When 用户按方向键或 Home End
    Then 焦点和活动标签按可用项目切换
    And 关闭文档仍遵循原保存和冲突规则

  Scenario: WB-06 简易模式使高级目的地回到文稿
    Given 用户正在查看文献或版本
    When 用户启用简易模式
    Then 返回仍然存在的文稿
    And 高级入口不再触发隐藏操作
    When 用户关闭简易模式
    Then 当前模式的可用工具重新显示且至少一个工具处于选中状态

  Scenario: WB-07 面板和缩放保留用户布局
    Given 用户已调整导航和工具栏宽度
    When 用户折叠一侧或调整窗口缩放
    Then 另一侧状态不被翻转
    And 临时窄视口不覆盖保存的栏宽
    And 用户可回到文稿并继续输入

  Scenario: WB-08 版本视图保留项目导航
    Given 当前项目存在文档与版本记录或明确的无仓库状态
    When 用户查看版本
    Then 当前项目导航仍可见
    And 版本内容使用真实仓库数据或明确的空状态
    When 用户返回文稿
    Then 当前编辑内容仍被保留
