import { describe, expect, it } from 'vitest';

// Wave 0 stub（Task 1 建脚手架，Task 2 转绿）：useVaultStore 尚未实现。
describe.todo('useVaultStore', () => {
  it('openVault sets current vault and tree', () => {
    expect(true).toBe(true);
  });
});
