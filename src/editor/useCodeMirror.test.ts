import { describe, expect, it } from 'vitest';

// Wave 0 stub（Task 1 建脚手架，Task 3 转绿）：useCodeMirror 尚未实现。
describe.todo('useCodeMirror', () => {
  it('mounts exactly one EditorView under StrictMode double-invoke', () => {
    expect(true).toBe(true);
  });
});
