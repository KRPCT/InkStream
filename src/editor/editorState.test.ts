import { describe, expect, it } from 'vitest';

// Wave 0 stub（Task 1 建脚手架，Task 2 转绿）：editorState 缓存尚未实现。
describe.todo('editorState cache', () => {
  it('openFile reuses cached state on second open', () => {
    expect(true).toBe(true);
  });
});
