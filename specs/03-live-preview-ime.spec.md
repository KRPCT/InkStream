# Phase 3 中文 IME 手测清单（永久回归门 D-13）

> **本清单是 EDIT-06 永久回归门。** 它是全项目最高风险件（`view.composing` 冻结纪律）的护栏：
> 一旦冻结回退即吞字 / 候选窗跳位 / 上屏错乱。
>
> **本阶段及 Phase 4 / 5 / 8 / 9 的所有装饰类变更，合并前必须逐项勾验通过。** 任何触及
> CodeMirror 装饰层（块级 StateField / 行内 ViewPlugin / atomicRanges）的改动都要重跑本清单。
>
> 与 `specs/01-workbench.spec.md` 同构：入库公开可审计、人工逐项勾验。在力所能及范围内已补
> Vitest 模拟 `CompositionEvent` 的自动回归桩以减人工负担，但**人工清单始终是硬门**——
> 自动桩无法复现真实候选窗行为，不可替代真机逐项勾验。

## 0. 范围与平台门

- **Windows 为本阶段硬门**：下方 4 输入法 × 4 场景类矩阵在 Windows 真机逐项勾验通过，方可合并装饰类变更。
- **表格就地编辑（方案 B 子编辑器）IME 专项**：见 §1 专项框 + §4.5——子编辑器是独立 IME 宿主，须单独逐项勾验
  （上屏不跳末尾 B1 / 撤销不跳顶 B2 / 跨格续写 / 对齐列编辑 / 空行高），触及 tableCellEditor / TableWidget / blockField 的改动须重跑。
- **macOS（注音 / 拼音）、Linux（fcitx / ibus）留 Phase 12 打包阶段补测**——本阶段不阻断，届时以同结构矩阵回归。
- 执行时机：Live Preview 链接 / 装饰落地后（Wave 2 之后）逐项执行；jsdom 无法复现真实候选窗行为，故全部为真机手测。

## 1. 通用判据（每格勾验四项，全部满足才算通过）

每个「输入法 × 场景」单元格在输入中文时，须同时满足以下四项，缺一即判失败：

1. **候选窗不跳位**：组合期（`compositionstart`→`compositionend`）候选窗位置稳定，不随装饰重算闪移。
2. **上屏无错乱**：`compositionend` 上屏的文字顺序、内容与所选候选完全一致，无错位 / 重复 / 顺序颠倒。
3. **无吞字**：组合期与上屏后均无字符丢失（含连续快速输入、组合中途删改）。
4. **刷新无字号 / 行高跳动**：`compositionend` 后装饰刷新瞬间，所在行字号、行高、缩进零位移
   （承接 UI-SPEC「显标记、保排版」不跳契约；中文正文行高不低于 1.5）。

> **重复同音节专项（EDIT-06 跨组合竞态回归门，5c8f826 残留 bug）**：连续快速键入**重复同音节**
> （如「咕咕咕」连打三遍上屏）——三个字必须全部成功上屏，无一被吞；随后键入**不同音节**（如「咕噜」）
> 不得回归。此专项用于压住 compositionend 强刷微任务被压进下一组合 `composing===0` 启动窗的竞态：
> 第 N 次上屏的陈旧强刷若在 N+1 组合启动窗派发，会撞 `observer.clear()` 丢弃 N+1 尚未 flush 的上屏
> mutation 而吞字。**自动桩绿 ≠ 真机 IME 已修**——jsdom 无真实 inputState.composing 三态与 MutationObserver
> 记录，本专项仍以 Windows + WebView2 真机微软拼音 / 搜狗拼音逐项手测为唯一验收。

> **表格单元格子编辑器专项（方案 B，TABLE-WYSIWYG / commit 0b451b3）**：表格就地编辑用嵌套子 EditorView
> 承载单元格——它是与主编辑器**并存的独立 IME 宿主**（独立 contentDOM、子组合事件经 `stopBubblingToMain`
> 不上达主门、组合期**每个 compositionupdate 即时 commit 回写主 doc**）。此路径不走主编辑器组合门，第 1 节
> 四判据之外**额外**须满足（缺一即失败）：
> 1. **上屏后光标停插入点**：在单元格已有文字**中部**用拼音输入、按空格确认上屏后，光标停在刚上屏文字之后，
>    **不弹到单元格末尾**（B1 真因：commit 重建子编辑器 + focusSub 落 doc.length；已修为复用子实例 + 保选区）。
> 2. **撤销不跳顶**：单元格内 `Ctrl+Z` 在**本格**撤销，视图**不跳文档顶**（B2：本地优先撤销 + 进格锚主选区）。
> 3. **跨格续写不串味**：连续中文上屏 + `Tab` 跨格续写，无吞字、无内容串格、无光标错位。
> 4. **对齐列编辑一致**：在居中 / 右对齐列单元格编辑时文字跟随该列对齐，上屏后与非活动格渲染一致（Wave2/3 A）。
>
> **真机硬门**：上述均为方案 B 子编辑器特有路径，jsdom（含 tableCellEditor.test.ts 的 `CompositionEvent` 桩）
> 不驱动子 view 真实组合状态机、不复现「空格确认上屏」的真实时序——故以 Windows + WebView2 真机逐项手测为唯一验收。

## 2. 输入法覆盖（D-11，4 项）

| 代号 | 输入法 | 选取理由 |
|------|--------|----------|
| IME-A | 微软拼音 | 系统内置基准，最常见路径 |
| IME-B | 搜狗拼音 | 市占第一，第三方候选窗行为差异大 |
| IME-C | 双拼模式 | 码长 / 上屏时序与全拼不同 |
| IME-D | 手写 / 语音面板 | 非标准一次性上屏路径，绕常规组合事件序列 |

## 3. 场景 / 装饰位置覆盖（D-12，4 类）

| 代号 | 场景类 | 具体落点 |
|------|--------|----------|
| S-1 | 行首块标记处 | `##` / `-` / `>` / `1.` 后紧跟输入中文 |
| S-2 | 行内标记内 | `**` 加粗内 / `` ` `` 行内代码内 / `[]()` 链接文本内 |
| S-3 | 空文档 / 空行首字 | 无前文上下文的合成；含列表空项、表格空单元格 |
| S-4 | 多行块内续写 | 表格单元格 / 引用块 / 列表项续行的连续输入 |

## 4. 勾验矩阵（4 输入法 × 4 场景 = 16 格）

> 每格勾验前先切到对应输入法，在对应落点输入一段中文（建议含多字词、连续句、组合中途删改各一次），
> 逐项对照第 1 节四判据。通过则勾选。

### 4.1 微软拼音（IME-A）

- [ ] IME-A × S-1 行首块标记处：`##` / `-` / `>` / `1.` 后输入中文，候选窗不跳位 / 上屏无错乱 / 无吞字 / 刷新无字号行高跳动。
- [ ] IME-A × S-2 行内标记内：`**`…`**` / `` ` ``…`` ` `` / `[`…`]()` 内输入中文，四判据全过。
- [ ] IME-A × S-3 空文档 / 空行首字：空文档首字、列表空项、表格空单元格输入中文，四判据全过。
- [ ] IME-A × S-4 多行块内续写：表格单元格 / 引用块 / 列表项续行连续输入中文，四判据全过。

### 4.2 搜狗拼音（IME-B）

- [ ] IME-B × S-1 行首块标记处：四判据全过。
- [ ] IME-B × S-2 行内标记内：四判据全过。
- [ ] IME-B × S-3 空文档 / 空行首字：四判据全过。
- [ ] IME-B × S-4 多行块内续写：四判据全过。

### 4.3 双拼模式（IME-C）

- [ ] IME-C × S-1 行首块标记处：四判据全过（重点核对短码上屏时序无吞字）。
- [ ] IME-C × S-2 行内标记内：四判据全过。
- [ ] IME-C × S-3 空文档 / 空行首字：四判据全过。
- [ ] IME-C × S-4 多行块内续写：四判据全过。

### 4.4 手写 / 语音面板（IME-D）

- [ ] IME-D × S-1 行首块标记处：四判据全过（重点核对一次性上屏不触发装饰错算）。
- [ ] IME-D × S-2 行内标记内：四判据全过。
- [ ] IME-D × S-3 空文档 / 空行首字：四判据全过。
- [ ] IME-D × S-4 多行块内续写：四判据全过。

### 4.5 表格单元格子编辑器专项（方案 B，独立 IME 宿主；至少 IME-A 微软拼音 + IME-B 搜狗逐项）

> 在一张有内容的表格里逐项验：先点入单元格激活子编辑器，对照第 1 节四判据 **+ 上方「表格单元格子编辑器专项」四项**。

- [ ] 中部插入上屏不跳末尾：单元格已有「年龄」，在「年」「龄」之间用拼音输入「与」并空格确认 → 显示「年与龄」，光标停在「与」之后（不弹末尾）。（B1）
- [ ] 单元格内撤销不跳顶：在文档很靠下的表格单元格输入中文后按 `Ctrl+Z` → 本格内容回退、视图停在该表格处不跳文档顶。（B2）
- [ ] 跨格连续续写：单元格 A 输入中文 → `Tab` 到 B 续输中文 → 内容各归各格、无吞字、无串格。
- [ ] 对齐列编辑一致：把某列设居中 / 右对齐后进入该列单元格输入中文，编辑中文字即跟随对齐，空格确认上屏后与同列非活动格渲染一致。（Wave2/3 A）
- [ ] 空单元格行高一致：工具条「插入行」后在新空行单元格输入中文上屏，空行高度自始至终与内容行一致（不偏矮）。（Wave2/3 B）

### 4.6 Fenced 公式块源码编辑专项（Phase 5，```math / ```latex / ```typst；至少 IME-A 微软拼音 + IME-B 搜狗）

> 光标进公式块即还原源码（块级 replace 装饰 skip，**主编辑器**直接编辑源码——非子编辑器，走主组合门，与表格子编辑器
> 路径不同）。在源码内输入中文（typst `#text[中文]` / latex `\text{中文}` / 注释等），对照第 1 节四判据 **+** 下列专项：

- [ ] 块内源码输中文不吞字：进 ```typst / ```latex / ```math 块，在源码内连续输入中文（多字词 + 组合中途删改），无吞字、上屏无错乱。
- [ ] 组合期块不闪渲染：块内输中文组合期间，该块**保持源码态不闪回渲染**（blockField `isComposingTr` 映射旧装饰不重建）；`compositionend` 后才刷新。
- [ ] 进/出块切换不被组合打断：光标在「块外渲染 ↔ 块内源码」间移动时，边界重建走 `isComposingTr` 门，组合期不撕装饰吞字。
- [ ] typst 异步编译不干扰输入：typst 块内输中文（200ms 防抖编译）期间输入流畅、无吞字（编译在 Worker，不阻塞主线程 IME）。
- [ ] `/math` `/latex` `/typst` 触发器插块后续输中文：补全 apply 插块 + 光标落空行后直接输中文，无吞字、光标不弹位（apply 零 `view.focus`）。

## 5. 自动回归桩（减负，非硬门）

- [ ] 模拟 `compositionstart` → 输入 → `compositionend` 的 Vitest 桩存在并通过：断言组合期装饰暂停**语法树重算**、
  组合期 docChanged 时旧装饰经 `tr.changes` **映射跟随位移**（非返回未映射旧集）、`compositionend` 后推迟一个
  微任务统一刷新一次（覆盖 `view.composing` / `isFrozen` 双判 + CM6 原生 `input.type.compose` userEvent 路径）。
  - 验证：自动（对应 CM 装饰扩展的配对测试，`pnpm test` 全绿即覆盖）。
  - 说明：此桩仅减少人工负担，**不替代**第 4 节真机矩阵；自动桩绿不构成回归门通过。
- [ ] 表格子编辑器（方案 B）配对桩存在并通过：`tableCellEditor.test.ts` 组合期门桩（组合中子→主不 commit、
  end 后落 doc）+ B1/B2 回归桩（连续 commit 复用同一子编辑器实例不重建 / 本地优先撤销 undo(sub) 先撤本格 /
  进格锚主选区落表内 / 活动格跟随列对齐）。
  - 验证：自动（`pnpm test` 全绿即覆盖）。
  - 说明：同样**不替代** §4.5 真机专项——jsdom 不驱动子 view 真实组合与「空格确认上屏」时序。

> **真因结论（2026-06-12，WebView2 平台限制，非装饰层）**：经真机 CDP 直连确证，中文 IME 仅在**真实指针进入
> 编辑器**时才武装 OS IME/TSF；任何 programmatic 路径（`view.focus()` / 原生 `ICoreWebView2Controller::MoveFocus` /
> EditContext）都不武装 → 打开文件后**首次**程序化聚焦的中文组合丢字。装饰层（freeze/map、Option 2 活动行纯源码）
> 经 CDP 证实**无辜**——IME 一旦工作，装饰层不打断它（即第 5 节自动桩断言的契约成立）。故对策为**不程序化抢焦点**
> （去 auto-focus，由用户点击编辑器自然落焦）；该 WebView2 限制记入 `.planning/CONSTRAINTS.md §8`，深层 click-free
> IME 健壮性并入 UX 重做里程碑。
>
> **诊断脚手架已移除**：定位期间曾加的 DEV-ONLY `[IME-TRACE]` 追踪器（前端 console + Rust `ime_trace_append` 落盘）、
> 原生 `arm_webview_ime` 武装、程序化 auto-focus 三者在结论确定后一并移除（仓库纯净硬约束）。需复诊按 git 历史还原即可。

> **⚠️ 真机为 EDIT-06 唯一验收门（jsdom 无法复现 composition，这正是本 bug 曾经带病上线的原因）。**
> jsdom 的 `CompositionEvent` 桩**不驱动**浏览器真实 IME 组合状态机：它无法复现「合成中文本节点」、
> 候选窗锚点、`compositionend` 上屏 flush 与 `observer.clear()` 的竞态——而本 bug 的两处根因恰好只在
> 真实 composition 下暴露：(B) 组合期返回**未映射**的旧装饰集 → CM6 `findChangedDeco` 伪重建合成中 DOM →
> Chromium 中止 IME 吞字；(A) `compositionend` **同步**派发事务 → `observer.clear()` 丢弃尚未 flush 的上屏
> mutation → 已选候选字反被吞。自动桩只能断言「映射语义」与「无同步派发 / 推迟一次刷新」的**契约**，
> **不能**断言真实候选窗不吞字。故 **Windows + WebView2 真机、微软拼音 / 搜狗拼音跨装饰内容多候选连续输入
> 必须逐项手测**（第 4 节矩阵），方可判 EDIT-06 通过。

## 6. 失败处置

任一单元格四判据有一项不满足，即判本次装饰变更不可合并：

1. 定位回退点：多为组合期未短路装饰重建（`view.composing` / `isComposing` 漏判）或 `compositionend` 刷新破坏盒模型；
   表格子编辑器路径（§4.5）多为子→主 commit 触发重建未保子实例/选区，或撤销委派/锚选区回退。
2. 修复后**重跑整张矩阵**（不可只补失败格——冻结纪律是全局不变量）。
3. 修复与重验结论记入对应阶段 SUMMARY 的偏差 / 验证小节。

## 7. 验收签核记录（每次跑矩阵填一行，留审计轨迹）

> 第 4 节 16 格 + §4.5 专项每全过一轮，在此记一行。结果列填「全过」或「失败：<格号/项>」。
> 装饰类变更合并前须有一行覆盖该提交且结果为「全过」。

| 日期 | 验证提交(SHA) | 平台 / WebView2 | 输入法 + 版本 | 结果 | 备注 |
|------|---------------|-----------------|---------------|------|------|
| （待填） | | Windows / 148 Fixed Runtime | 微软拼音 / 搜狗 | | |
| （待填，Phase 5）| 8849568 / 3286eb7 / 21d33b7 | Windows / 148 Fixed Runtime | 微软拼音 / 搜狗 | | §4.6 fenced 公式块（math/latex/typst）源码 IME 待真机逐项 |
